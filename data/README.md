# Data

The original `musicData.csv` file is not included in this repository because it was provided for course use.

To run the project, place `musicData.csv` in the main project folder or update the file path in the Python script.
